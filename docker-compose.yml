services:
  cmi:
    container_name: cmi
    build:
      context: .
      dockerfile: cmi-back/Dockerfile
    ports:
      - "80:4000"
    volumes:
      - ./cmi-back/data:/app/data
    restart: unless-stopped
